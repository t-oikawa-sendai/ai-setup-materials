# AGENTS.md

## 1. 目的

このリポジトリは、職業訓練校の生徒へ配布するAI活用教材・AI Persona・運用資料を整備するための正本である。

AIは会話内容を正本として扱わず、リポジトリ内の文書を基準に作業する。

## 2. 作業開始時の必須確認

1. `project-notes/CURRENT.md` を最初に読む。
2. `CURRENT.md` の `CONFIRMED` を確定事項として扱い、再検討しない。
3. `CURRENT TARGET` と `NEXT ACTION` を現在の作業地点とする。
4. 詳細な判断理由が必要な場合は、`REFERENCES` に記載された文書を確認する。
5. 既存の決定記録を更新する場合は、対象文書の既存CONFIRMED事項を全文確認してから統合する。

## 3. 基本原則

- 事実・確定事項・未確認事項・仮定を分離する。
- 確定事項を推測で変更しない。
- ユーザーが明示的に決定していない内容を、推論・一般論・AIの補完だけでCONFIRMEDとして記録しない。
- 解釈が複数あり得る場合、既存文書と矛盾する場合、不明点がある場合は、勝手に解消せずユーザーへ確認する。
- 不要なファイル、機能、ルール、抽象化を増やさない。
- 重要な要求、要件、設計判断、実装内容、検証結果、レビュー結果は文書として残す。
- 明示的に確定した要求・設計・責務境界・運用方針は、会話だけに残さず、適切な `project-notes/` へ速やかに反映する。
- 既存の決定記録へ新しい決定を統合する場合、過去のCONFIRMED事項を欠落・縮退させない。
- Evidenceが必要な工程では、根拠を追跡できる形で明示する。
- 対象外のファイルや仕様を勝手に変更しない。
- 既存の正本と矛盾した場合は、勝手に解消せず停止して判断を求める。

## 4. 文書の責務

- `AGENTS.md`: このリポジトリでAIが守る恒久ルール。
- `project-notes/CURRENT.md`: 現在地点、確定事項、次作業を示す作業状態のSSOT。
- `project-notes/YYYY-MM-DD-*.md`: 重要な判断理由・決定履歴。
- `personas/education/`: 生徒向けAI Personaの正本。
- `personas/reference/`: 実務構成の参考Persona。

## 5. CURRENT.md とGitHub履歴の運用

- 通常作業中はローカルGitリポジトリの `project-notes/CURRENT.md` を作業正本として使用する。
- 作業中に現在地点・対象・次作業が変わった場合は `CURRENT.md` を更新する。
- GitHubを通常の細かな作業状態すべてのリアルタイム同期基盤として扱う必要はない。
- ただし、ユーザーが重要な要求・設計・責務境界・運用方針を明示的に確定した場合は、セッション終了まで放置せず、適切な決定記録をこまめにGitHubへ保存する。
- その決定によって現在地点・次作業も変わる場合は `CURRENT.md` も更新する。
- 次の場合は `CURRENT.md` を含む必要な変更を commit / push する。
  - 重要な決定事項を記録したとき
  - 作業終了時
  - 別端末へ作業を移す前
  - ローカルリポジトリを直接参照できないAIへ引き継ぐ前
  - 復旧用チェックポイントを残す必要がある重要な節目
- 別端末で再開する場合は、作業開始前に最新状態を pull する。

## 6. 作業終了時

1. 完了事項を確認する。
2. `project-notes/CURRENT.md` を最新状態へ更新する。
3. セッション中の新しい重要決定がすべて適切な決定記録へ反映済みか確認する。
4. 既存決定記録を更新した場合、過去のCONFIRMED事項が欠落していないか確認する。
5. 必要な検証結果を確認する。
6. commit / push を行い、GitHubへ終了地点を保存する。

## 7. 禁止事項

- `CURRENT.md` を読まずに継続作業を開始しない。
- `CONFIRMED` を再調査・再設計しない。
- 会話履歴だけを根拠に確定事項を上書きしない。
- ユーザー未確認の推論を決定事項として記録しない。
- 新しい決定記録への更新時に、既存の確定事項を要約しすぎて欠落させない。
- バックアップ専用領域を現行正本・運用必須資産として扱わない。
- 秘密情報をコード、文書、ログ、公開物へ記載しない。
