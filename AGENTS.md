# AGENTS.md

## 1. 目的

このリポジトリは、職業訓練校の生徒へ配布するAI Personaと、そのPersonaを実際に利用するために直接必要な設定・運用資料を整備するための正本である。

対象は次とする。

- Persona定義
- Personaの選択・使い分け
- Persona間の責務境界
- Persona利用に必要なREADME
- AIサービスへのPersona設定方法
- Personaの導入・切替・利用手順
- Persona運用に直接必要な設定資料

次は対象外とする。

- 一般的な生成AI学習教材
- 要求定義・要件定義・各種設計などの一般教材
- アプリ開発用7文書
- 他Repositoryを正本とする教材本文・設計文書

設計ドキュメント標準で扱う7文書は別Repositoryで管理する。このリポジトリへ複製・機械的生成を行わず、Persona領域へ混在させない。

この責務限定の背景意図は、Personaとその利用に直接必要な資料へ範囲を絞り、性質の異なる教材本文や設計文書の混在によって正本の所在が曖昧になることを防ぐことである。

AIは会話内容を正本として扱わず、リポジトリ内の文書を基準に作業する。

## 2. 作業開始時の必須確認

### 2.1 Git同期ゲート

Repositoryに関する作業では、ユーザーから個別の同期指示がなくても、次を最初に行う。

1. 実作業用ローカルRepositoryを特定する。
2. 現在のbranch、remote、ローカルHEAD、未commit変更の有無を確認する。
3. remoteの最新状態を取得し、リモートHEADとローカルHEADを比較する。
4. ローカルがcleanで安全に追従できる場合は、fast-forwardのみで最新状態へ同期する。
5. 同期後のローカルHEADとリモートHEADが一致していることを確認する。

次の場合は同期済みと扱わず、本作業を開始しない。状態と停止理由を報告し、必要な判断をユーザーへ求める。

- 実作業用ローカルRepositoryを特定できない。
- remoteまたは対象branchを確認できない。
- 未commit変更がある。
- ローカルとremoteが分岐している、またはfast-forwardできない。
- fetch、pull、権限、network等の問題で最新状態を確認できない。

未commit変更を破棄・退避・上書きして同期を強行しない。

作業報告には、少なくとも次の同期Evidenceを含める。

- 対象Repository
- 作業branch
- 作業開始時のローカルHEAD
- 確認したリモートHEAD
- 未commit変更の有無
- 同期結果、または同期を停止した理由

### 2.2 文書確認

Git同期ゲート通過後に、次を行う。

1. `project-notes/CURRENT.md` を最初に読み、現在地点（完了事項・作業中・次工程）を確認する。
2. 対象成果物MDの本文を現行仕様として扱い、確定済み内容を再検討しない。
3. なぜその仕様になったかを確認する必要がある場合は、対象成果物MDの `Decision & Rationale` を読む。§5.4の公開・教材向け文書の分離例外が適用される文書では、対象MDが示す対応先のDecisionファイルを読む。
4. 2026-08-23より前の判断経緯が成果物MDに無い場合のみ、`project-notes/YYYY-MM-DD-*.md` の履歴資料を参照する。履歴資料を現行仕様として扱わない。
5. 既存の成果物MDを更新する場合は、対象文書の現行本文と既存の `Decision & Rationale` を確認してから統合する。§5.4の分離例外が適用される文書では対応するDecisionファイルも確認する。過去のDecisionを欠落・縮退させない。
6. `CURRENT.md` が `RECOVERY / RECONSTRUCTION` の場合は、通常作業より復旧を優先する。
7. Archive、および `CURRENT.md` の「現行として使用しない文書」を現行仕様として使わない。

## 3. 基本原則

- 事実・確定事項・未確認事項・仮定を分離する。
- 確定事項を推測で変更しない。
- ユーザーが明示的に決定していない内容を、推論・一般論・AIの補完だけで確定事項として記録しない。
- 解釈が複数あり得る場合、既存文書と矛盾する場合、不明点がある場合は、勝手に解消せずユーザーへ確認する。
- 不要なファイル、機能、ルール、抽象化を増やさない。
- 重要な要求、要件、設計判断、実装内容、検証結果、レビュー結果は文書として残す。
- 明示的に確定した要求・設計・責務境界・運用方針は、会話だけに残さず、対象成果物MDへ即時反映する。記録基準に該当する場合は、原則として同じ成果物MDの `Decision & Rationale` へ判断理由も追記する。§5.4の公開・教材向け文書の分離例外が適用される場合のみ、対応する管理用Decisionファイルへ記録する。
- 既存の成果物MDへ新しい決定を統合する場合、過去の確定事項と既存の `Decision & Rationale`、または分離されたDecisionファイルの履歴を欠落・縮退させない。
- Evidenceが必要な工程では、根拠を追跡できる形で明示する。
- 対象外のファイルや仕様を勝手に変更しない。
- 既存の正本と矛盾した場合は、勝手に解消せず停止して判断を求める。

## 3.1 AI能力に関する安全前提

このRepositoryでは、AIの能力を過信しない。

AIは、重要情報を単独で完全・正確に保持、統合、更新できる保証がないものとして扱う。

特に次の失敗を起こし得る。

- 過去の決定を取りこぼす
- 新旧決定の上書き関係を取り違える
- 旧情報を現行情報として復活させる
- 複数文書の統合時に確定事項を欠落させる
- ファイルの存在と現在採用を混同する
- 推論・補完を確定事項へ混入させる
- 自分が正しく整理できていると誤認する
- 記録要否を自己判断して決定事項を会話空間へ残す

そのため、重要情報資産の保全をAIの記憶・会話履歴・自己判断に依存しない。

安全策：

1. 名称変更、役割変更、採用・廃止、置換がある場合は、時系列と上書き関係を確認する。
2. 重要事項を `CURRENT / SUPERSEDED / UNRESOLVED` に分離する。
3. 既存ファイルの存在だけで現行採用と判断しない。
4. 統合前に競合・矛盾・旧決定復活の有無を確認する。
5. AI側で確認可能な事項をユーザーへの個別質問へ転嫁しない。
6. 資料だけで一意に決まらない事項のみユーザーへ確認する。
7. 「統合済み」「確定」「正本」と宣言する前に、最新性・上書き関係・欠落を検証する。
8. 誤統合が発覚した場合、原因説明だけで復旧扱いにせず、信頼できる資料から再構築する。
9. User決定後は、対象成果物MDへ即時反映し、GitHubへ保存してから次工程へ進む。§5.4の分離例外を除き、別管理ログへ集約して正本更新を後回しにしない。

安全前提（AI能力を過信しない、誤統合時は再構築する）の詳細は `project-notes/2026-08-21-ai-information-asset-safety.md` を参照する。決定事項の記録先は、2026-08-23以降、同記録ではなく本ファイルの正本管理ルールに従う。

## 3.2 背景意図の保全

`背景意図` は、要求・ルール・設計判断・文書構成・用語の表面上の内容だけではなく、なぜそれが必要か、何を防ぎ、どの価値・制約・責務境界を守るために存在するかを示す情報である。

このRepositoryでは、背景意図を重要情報資産として扱う。

1. 重要な要求・ルール・設計判断を記録するとき、将来HumanまたはAIが表面上の文言だけを機械的に適用すると誤解する可能性がある場合は、背景意図も併記する。
2. 背景意図は、ユーザーが明示した内容または追跡可能なEvidenceで確認できる内容だけを記録する。AIが利用者の内心や未明示の目的を推測して補完しない。
3. 文書の変更、統合、簡略化、置換、例外適用では、表面上の要件だけでなく、確認済みの背景意図が維持されているか確認する。
4. 後続決定で実現方法が変わった場合、背景意図まで変更されたのか、同じ背景意図を別の方法で実現するのかを分離して記録する。
5. 表面上の指示に複数の解釈があり、背景意図を正本・決定記録・Evidenceから一意に確認できない場合は、推測せずユーザーへ確認する。
6. 背景意図を理由に、明示されていない要求・必須工程・成果物を勝手に追加しない。
7. 背景意図は、対象成果物MDの本文、または記録基準に該当する場合の `Decision & Rationale` の Reason に記録する。§5.4の分離例外が適用される場合は、対応する管理用Decisionファイルの Reason に記録する。会話や追跡不能な別ログだけに残して正本から外さない。

## 3.3 ASKME運用ルール

- ASKMEは、判断が必要な未決事項が存在する場合のみ使用する。
- 既に決定済みの事項、対象成果物MDの本文、`Decision & Rationale`、分離されたDecisionファイル、`CURRENT.md` の現在地点に存在する事項をASKME対象にしない。
- AI側で確認可能な事項を、不要なユーザー確認へ転嫁しない。
- ASKME対象が存在しない場合、確認停止せず既定の作業を継続する。
- 作業手順、既存決定事項、確定済み優先順位をASKMEとして再確認しない。
- AIは「念のため確認」による作業停止を避ける。
- 判断が必要になった場合のみ停止し、必要最小限の確認を行う。

背景意図：

ASKMEは品質確保のための判断ゲートであり、作業停止のための確認機構ではない。
不要な確認増加による作業停滞を防ぎ、確定事項に基づく継続作業を保証する。

## 4. 文書の責務

- `AGENTS.md`: このリポジトリでAIが守る恒久ルール。最新の作業規則と、その規則に関する `Decision & Rationale` を保持する。
- 対象成果物MD（Persona、README、設定・運用資料等）: 最新仕様、最新構成、最新手順の正本。記録基準に該当する判断理由は、原則として同じファイルの `Decision & Rationale` に保持する。§5.4の分離例外が適用される公開・教材向け文書では、本文は対象成果物MD、判断履歴は対応する管理用Decisionファイルを正本とする。
- 公開・教材向け文書の管理用Decisionファイル: §5.4の分離例外が適用された対象成果物について、判断履歴だけを保持する管理用正本。`project-notes/` 配下に置き、対象成果物MDからHTMLコメントで対応先を示す。対象成果物の最新仕様・本文を代替しない。
- `project-notes/CURRENT.md`: 現在地点のみ。完了事項、作業中、次工程を記載する。詳細設計や判断理由は記載しない。
- `project-notes/YYYY-MM-DD-*.md`: 2026-08-23より前の判断経緯、復旧、監査Evidence、実装指示などの履歴・内部記録。新規の設計決定の代替正本としては使用しない。削除せず履歴として保持する。復旧・監査Evidenceなど、成果物本文以外の内部記録が必要な場合に限り新規作成できる。
- `personas/education/`: 生徒向けAI Personaの正本。
- `personas/reference/`: 実務構成の参考Persona。
- `personas/<区分>/setup/`: 各AIサービスの設定・運用資料。実際の文書を作成するときに配置し、記録目的だけで空ディレクトリやダミーファイルを作らない。

Persona本文には、AIサービス固有のセットアップ手順・配置方法・操作手順を混在させない。設定・運用資料はPersonaとは別文書とし、設定方法と配置場所がAIサービスごとに異なるためAIサービス単位で管理する。

## 5. 正本管理と決定反映

### 5.1 基本方針

決定事項の扱いを次の流れに固定する。

```text
検討
 ↓
User決定
 ↓
対象成果物MDへ即時反映
 ↓
必要なDecision & Rationaleを記録
 ↓
GitHub保存
 ↓
次工程
```

採用しない流れ：

```text
会話
 ↓
AI判断
 ↓
必要と思われた事項のみ記録
 ↓
GitHub
```

成果物本文は対象成果物MDを正本とする。判断履歴は原則として同じMDへ保持する。§5.4の公開・教材向け文書の分離例外が適用される場合のみ、判断履歴を対応する管理用Decisionファイルへ分離できる。

### 5.2 正本

正本は対象成果物MDファイルである。

例：

- `GEMINI_GEM_SETUP.md`
- `personas/education/GEM_REVIEWER.md`
- `personas/education/README.md`
- 本ファイル（`AGENTS.md`）

役割：

- 最新仕様
- 最新構成
- 最新手順

を保持する。

§5.4の分離例外が適用される場合も、本文・最新仕様の正本は対象成果物MDであり、管理用Decisionファイルは判断履歴だけの正本とする。

### 5.3 CURRENT.md

役割は現在地点のみとする。

記載する：

- 完了事項
- 作業中
- 次工程

詳細設計や判断理由は記載しない。確定仕様の正本は対象成果物MDである。

### 5.4 Decision & Rationale

対象成果物MDには、記録基準に該当する場合、原則として次のセクションを追加または追記する。

```markdown
## Decision & Rationale

### 2026-08-23
#### Gemini Gem設定資料の管理方式

Decision:
Gemini Gem設定資料は1資料管理とする。

Reason:
4Gem共通設定であり、重複管理を防止するため。

Rejected:
Persona別4資料方式
```

目的は、「現在どうなっているか」だけではなく、「なぜそうなったか」を追跡可能な形で保持することである。

既存の `Decision & Rationale` がある場合は、新しい日付エントリを追記する。過去エントリを削除・要約して欠落させない。

空の `Decision & Rationale` セクションを、記録対象がない文書へ先に作らない。

#### 公開・教材向け文書の分離例外

生徒・利用者が直接読むことを目的とする公開教材・公開参考資料では、内部の判断履歴が本文の可読性を損なう場合、`Decision & Rationale` を対象成果物MDから `project-notes/` 配下の管理用Decisionファイルへ分離できる。

適用条件：

1. 対象が、生徒・利用者が直接読む教材・参考資料として明示されていること。
2. 分離対象であることがUser決定、または既存の正本ルールで明示されていること。AI判断だけで新たな分離対象へ変更しない。
3. 対象成果物MDの本文・最新仕様はそのMD自身を正本として維持すること。
4. 管理用Decisionファイルは判断履歴だけの正本とし、`project-notes/` 配下に置くこと。
5. 対象成果物MDには、対応する管理用DecisionファイルへのHTMLコメントを残すこと。利用者向け本文には管理用判断履歴を表示しない。
6. 分離時に既存のDecisionを削除せず、内容を欠落させず管理用Decisionファイルへ移すこと。
7. Persona本体、`AGENTS.md`、内部管理文書にはこの例外を自動適用しない。これらは原則として同一ファイル内に `Decision & Rationale` を保持する。

この例外は、判断履歴を消すための規則ではなく、公開本文の可読性と内部判断履歴の追跡可能性を両立するための規則である。

### 5.5 記録基準

AIによる曖昧な「重要判断」で記録要否を決めてはならない。次のいずれかに該当する場合に記録する。

1. 構造へ影響する変更
   - ファイル構成
   - ディレクトリ配置
   - 文書統合・分割
   - 章構成変更
2. 責務へ影響する変更
   - Persona責務
   - 資料の対象範囲
   - 含める内容／除外内容
3. 将来「なぜ？」が発生する変更
   - 章順変更
   - 削除理由
   - 方針変更
4. 複数案から選択した変更
   - 例：案A Persona別資料 / 案B 共通資料 → 採用B
5. 他成果物へ影響する変更
   - README、Persona、設定資料間の関係変更

### 5.6 記録不要

次は `Decision & Rationale` の対象外とする。

- 誤字修正
- 表現改善
- Markdown整形
- 意味を変えない文章修正

### 5.7 設計検討時とUser決定後

設計検討時に実施する。

- 選択肢整理
- メリット・デメリット提示
- 影響範囲提示

User決定後は、必ず次をこの順で行う。

1. 対象MDを特定する
2. 本文を更新する
3. 記録基準に該当する場合、原則として同じMDへ `Decision & Rationale` を追記する。§5.4の分離例外が適用される場合は、対応する管理用Decisionファイルへ記録する
4. GitHubへ保存する
5. 更新結果を報告する

禁止：

- 会話内だけで決定事項を保持する
- 後でまとめて記録する
- AI判断で記録不要とする
- §5.4の分離例外に該当しないのに、別途ログだけ作成して正本更新を後回しにする

## 6. GitHub保存

- 通常作業中はローカルGitリポジトリの成果物MDと `project-notes/CURRENT.md` を使用する。
- 作業中に現在地点・対象・次作業が変わった場合は `CURRENT.md` の現在地点だけを更新する。
- Userが要求・設計・責務境界・運用方針を明示的に決定した場合は、対象成果物MDへ即時反映し、セッション終了まで放置せずGitHubへ保存する。§5.4の分離例外が適用される場合は対応する管理用Decisionファイルも同時に保存する。
- 次の場合は、対象成果物MDと、現在地点が変わる場合の `CURRENT.md`、必要な管理用Decisionファイルを commit / push する。
  - User決定を成果物MDへ反映したとき
  - 作業終了時
  - 別端末へ作業を移す前
  - ローカルリポジトリを直接参照できないAIへ引き継ぐ前
  - 復旧用チェックポイントを残す必要がある重要な節目
- 別端末で再開する場合は、作業開始前に最新状態を pull する。

## 7. 作業終了時

1. 完了事項を確認する。
2. セッション中のUser決定が、対象成果物MDの本文へ反映済みか確認する。
3. 記録基準に該当する判断が、同じ成果物MDの `Decision & Rationale`、または§5.4の分離例外が適用される場合は対応する管理用Decisionファイルへ記録済みか確認する。
4. `project-notes/CURRENT.md` の現在地点を最新状態へ更新する。
5. 既存の成果物MDを更新した場合、過去の確定事項と既存の `Decision & Rationale`、または分離されたDecisionファイルが欠落していないか確認する。
6. 名称変更・役割変更・置換がある場合、旧決定が現行情報として復活していないか確認する。
7. 必要な検証結果を確認する。
8. commit / push を行い、GitHubへ終了地点を保存する。

## 8. 禁止事項

- Git同期ゲートを通過せずにRepository作業を開始しない。
- `CURRENT.md` を読まずに継続作業を開始しない。
- 対象成果物MDの確定済み本文を再調査・再設計しない。
- 会話履歴だけを根拠に確定事項を上書きしない。
- ユーザー未確認の推論を決定事項として記録しない。
- 会話内だけで決定事項を保持しない。
- 後でまとめて記録しない。
- AIの曖昧な判断で記録不要としない。
- §5.4の分離例外に該当しないのに、別途ログだけ作成して正本更新を後回しにしない。
- 新しい決定の反映時に、既存の確定事項や `Decision & Rationale`、分離されたDecisionファイルを要約しすぎて欠落させない。
- 旧決定と後続決定の関係を確認せず単純統合しない。
- ファイルが存在するだけで現行採用と決めない。
- 復旧未完了の文書を正本として扱わない。
- バックアップ専用領域を現行正本・運用必須資産として扱わない。
- 秘密情報をコード、文書、ログ、公開物へ記載しない。

## Decision & Rationale

### 2026-08-23

#### 文書管理ルール（正本・決定反映・CURRENT.mdの役割）

Decision:
決定事項は会話や別管理ログへ残さず、対象成果物MDへ即時反映する。成果物MD自身を最新仕様・最新構成・最新手順の正本とする。記録基準に該当する判断理由は、同じ成果物MDの `Decision & Rationale` に残す。`CURRENT.md` は現在地点（完了事項・作業中・次工程）のみを記載し、詳細設計や判断理由は記載しない。User決定の反映後にGitHubへ保存してから次工程へ進む。2026-08-23より前の `project-notes/YYYY-MM-DD-*.md` は削除せず履歴として保持するが、新規の設計決定の代替正本としては使用しない。

Reason:
最新状態の正本性を維持する。設計判断の過程を失わない。新チャット移行時でもGitHub上の成果物MDだけで過去判断を復元できるようにする。AIが記録要否を自己判断して決定事項を会話空間へ残すばらつきを防ぐ。

Rejected:
- 会話 → AI判断 → 必要と思われた事項のみ記録 → GitHub という流れ
- 決定事項を別管理ログへ集約し、正本更新を後回しにする方式
- AIの曖昧な「重要判断」による記録要否判断
- `CURRENT.md` に確定事項の全文を集約し、成果物MDと二重に正本化する方式

### 2026-09-01

#### User承認範囲の厳守と決定事項の即時記録

Decision:
Userが明示的に承認した変更だけを正本へ反映する。AIは、Userが明示的に決定した要求・設計・責務境界・運用方針を、既存の記録基準と正本管理ルールに従って対象成果物MDへ即時反映し、GitHubへ保存する。Userが承認していない内容、文書構造、章、運用方式をAI判断だけで正本へ追加しない。今回の `GEM_SOLUTION_PARTNER.md` では、Userが承認したPersona改善本文のみを保持し、AIが未承認で追加した `Decision & Rationale` は除去する。

Reason:
既存の `AGENTS.md` で、User決定後の即時反映、AI判断による未承認事項の確定禁止、対象外変更の禁止が既に定められていたにもかかわらず、AIがこれを遵守せず、`GEM_SOLUTION_PARTNER.md` へ未承認の `Decision & Rationale` を追加し、さらにUser決定事項の記録を `AGENTS.md` へ反映しなかった。既存ルールそのものを変更するのではなく、今回のUser決定と適用結果を正本へ記録し、承認範囲と記録責務を明確に残す。

Rejected:
- AIが必要と判断した内容をUser承認なしで正本へ追加する方式
- User決定事項を会話内だけに残し、正本への反映を後回しにする方式
- 未承認の文書構造変更を既存運用ルールの解釈だけで実施する方式

### 2026-09-01

#### Education Solution Partnerの設計回答精度を強化

**Decision:**

Education用Solution Partnerについて、職業訓練校のIT初学者が学習・演習・ポートフォリオ制作で利用する前提を維持しつつ、設計回答の精度を高める。

既存の「確定済み仕様を勝手に変更しない」「事実・未確認事項・仮定を区別する」「不要な機能や抽象化を追加しない」という原則は維持する。

その上で、次を追加する。

- Userが明示していない仕様を、一般論や実務慣行だけを理由に確定しない。
- 実装判断に必要な未確定事項は「要確認」または「仮定」として区別する。
- Userの目的、対象者、学習レベル、対象範囲に合わせ、学習に不要な実務レベルの厳格さや過剰設計を持ち込まない。
- User指定の出力形式、項目、順序を既定の出力形式より優先する。
- 回答前に、要求反映、仕様外追加、未確定事項、出力形式、回答内整合性を確認する。

**Reason:**

同一のWebアプリ仕様書生成プロンプトをPersonaあり／なしで比較した結果、Personaありでは仕様外追加の抑制や責務分離などに改善が見られた一方、未確定事項の補完、出力形式の完全遵守、回答内の整合性には改善余地が残った。

Education用Personaの目的は実務監査レベルの厳格さを導入することではなく、IT初学者が迷わず学習・設計・実装へ進める支援を行うことである。そのため、技術固有の詳細ルールや網羅的な監査項目を追加せず、設計回答に共通する最小限の確認ルールとして追加する。

**Rejected:**

- Spring FrameworkやDB設計など特定技術専用のチェック項目をPersonaへ固定する方式
- 実務監査レベルの網羅的な品質ゲートを導入する方式
- 現行Solution Partner Personaを全面的に再設計する方式
- 未確定事項を一般的なベストプラクティスで自動補完する方式

### 2026-09-01

#### 初学者向け成果物の導入説明と締めの説明を必須化

**Decision:**

Education用Solution Partnerが初学者向けの設計書・仕様書・説明資料を出力する場合、本文だけを提示せず、冒頭に資料の目的・何を示すかを簡潔に説明し、末尾にどこまで決まったか・未確定事項・次に行うことを簡潔に説明する。

これらは任意の親切説明ではなく、初学者が成果物の位置づけと次の行動を理解するための必須構成とする。

UserがMarkdown表など出力形式を指定している場合は、導入説明と締めの説明もその指定形式の中で表現し、形式遵守と教育上の説明を両立する。

**Reason:**

Version 1.1を適用したWebアプリ仕様書の再生成では、推測抑制や未確定事項の区別は改善した一方、冒頭の導入説明と末尾のまとめ・次工程案内が完全に欠落した。

職業訓練校のIT初学者向け教材では、設計情報そのものだけでなく、「何の資料か」「どこまで決まったか」「次に何をするか」を理解できることが学習上必要である。説明量を増やすことが目的ではなく、必要最小限の導入と締めを欠落させないことを目的とする。

**Rejected:**

- 導入説明・締めの説明を任意項目とする方式
- 初学者向けであっても設計情報だけを提示する方式
- 教育上の説明を理由に長い一般論や詳細解説を追加する方式
- User指定の出力形式を崩して導入説明・締めの説明を追加する方式

### 2026-09-01

#### 出力構造・未指定詳細・設計用語の安定性を強化

**Decision:**

Education用Solution Partnerの設計回答について、次を追加で必須化する。

- Userが指定した表・列・項目構造を途中で分割せず、必須内容を指定構造の外へ移動しない。
- Userが機能名や目的だけを示した場合、検索方式、画面遷移、権限、削除方式、データ項目、Framework機能などの未指定詳細を一般論から確定しない。
- 同じ作業内で既に確定した名称、クラス分割、責務、用語は、変更理由がない限り維持する。

**Reason:**

Version 1.2を適用したWebアプリ仕様書の再生成では、冒頭の導入説明と末尾のまとめ・次工程案内は改善した。一方で、Userが指定したプログラム一覧表がクラスごとに分割され、役割・担当処理・責務が表外へ出る問題が残った。

また、検索機能からLIKE検索を確定する、未指定の画面遷移やFramework機能を追加するなど、機能名から実装詳細を一般論で補完する傾向が残った。さらに、同じ用途のFormやViewの分割・名称が生成ごとに揺れる例も確認された。

これらは実務監査を強化する問題ではなく、初学者向け設計資料の分かりやすさ、再現性、プロンプト遵守に直接関係するため、技術固有ルールではなく汎用的な設計回答ルールとして追加する。

**Rejected:**

- Spring Framework専用のクラス構成・命名をPersonaへ固定する方式
- 特定の検索方式、削除方式、認証方式などを教材標準としてPersonaへ固定する方式
- 出力形式をAI側の読みやすさ判断で変更できるようにする方式
- 同じ作業内でもAIが毎回答で自由にクラス分割・命名を変更する方式

### 2026-09-11

#### Root READMEのDecision & Rationale分離

Decision:
ルート `README.md` は職業訓練校生徒向けの公開入口として可読性を優先し、READMEに関する `Decision & Rationale` は `project-notes/README_DECISIONS.md` へ分離する。README本文には判断履歴本文を置かず、管理上の対応先だけをHTMLコメントで保持する。`project-notes/README_DECISIONS.md` をルートREADMEの判断履歴の正本とする。

この決定はルート `README.md` に限る明示的例外であり、本ファイルにある「対象成果物MDと同じファイルに `Decision & Rationale` を保持する」「決定事項を別管理ログへ集約しない」という一般規定より優先する。他の成果物MDには適用しない。

Reason:
ルートREADMEは生徒・利用者が最初に読む教材入口であり、内部の設計判断履歴が長く続くと利用導線の可読性を損なう。一方、判断履歴自体は情報資産として保持する必要があるため、削除せず管理用 `project-notes/` へ分離する。

Rejected:
- READMEの `Decision & Rationale` を単純削除して履歴を失う方式
- すべての成果物MDの `Decision & Rationale` を一律に別管理へ移す方式
- `CURRENT.md` へREADME判断履歴を移す方式

### 2026-09-11

#### 生徒・利用者向け公開文書のDecision & Rationale分離

Decision:
Root READMEだけに限定していた `Decision & Rationale` 分離例外を、生徒・利用者が直接読むことを目的とする公開教材・公開参考資料へ拡張する。

公開・教材向け文書では、内部判断履歴が本文の可読性を損なう場合、対象成果物MDには利用者向け本文だけを保持し、判断履歴を `project-notes/` 配下の管理用Decisionファイルへ分離できる。対象成果物MDの本文・最新仕様はそのMD自身を正本とし、管理用Decisionファイルは判断履歴だけの正本とする。対象成果物MDには対応先をHTMLコメントで残す。

この分離はUser決定または既存の正本ルールで対象と明示された文書に限る。AIが独自判断で分離対象を増やしてはならない。Persona本体、`AGENTS.md`、内部管理文書は従来どおり同一ファイル内の `Decision & Rationale` を原則とする。

この決定は、2026-09-11の `Root READMEのDecision & Rationale分離` にある「ルートREADMEに限る」という適用範囲を更新し、§5.4の分離例外として一般化する。本ファイル内の同一ファイル保持規定と競合する場合は、この分離例外を優先する。

Reason:
生徒・利用者が直接読む教材・参考資料に内部の設計判断履歴を表示すると、教材本文の理解と利用導線を妨げる。一方で、判断履歴を削除すると情報資産と追跡可能性を失うため、公開本文と内部判断履歴を責務分離する。

Rejected:
- 公開教材・参考資料の `Decision & Rationale` を単純削除する方式
- すべての成果物MDから一律に `Decision & Rationale` を分離する方式
- AI判断だけで任意の文書を分離対象へ変更する方式
- 内部判断履歴を生徒向け本文へ表示し続ける方式

### 2026-09-13

#### Education Solution Partnerを Version 1.5へ正式改版

**Decision:**

Education用Solution Partnerを Version 1.5 / Status Approved へ正式改版する。既存Version 1.4への部分追記ではなく、Persona本文の整理と責務境界の明確化を含む改版とする。

- Solution Partnerは実装担当ではない。完成した実装コードの生成はCode Generatorの責務とする。
- 説明・比較・設計判断に必要な疑似コード、部分的なサンプルコード、SQL例、設定例、コマンド例、問題説明用の部分コード、差分例は禁止しない。
- コード可否の判断基準は分量ではなく、「そのまま提出・実行できる完成実装を生成しないこと」とする。
- 未決事項をAIが勝手に確定しない原則は維持する。
- UserがASKMEの決定を保留した場合は作業停止を続けず、暫定値を `仮定` と明示し、`補足A` に残し、後から変更可能であることを明示した上で次工程へ進む。
- `補足A` の管理対象に、従来の2分類に加えて `仮定を置いて進めた事項` を追加する。
- 冗長な重複規定（旧 `Design Response Check` を含む）を `Principles`、`ASKME`、`Output` 等へ統合し、Persona全体を簡潔化する。
- 学習用途・初学者向けという従来の背景意図は維持する。
- 現行どおり、Persona本文へ `Decision & Rationale` は追加しない。判断履歴は本ファイルへ記録する。

**Reason:**

完成実装の生成と設計説明用サンプルの混同を防ぐ。未決事項の勝手な確定は避けつつ、Userが決定を保留した場合に初学者が次工程へ進めなくなることを防ぐ。同じ確認規則の重複記載を解消し、適用漏れと解釈揺れを減らす。学習用途・初学者向けという従来の背景意図は維持し、実現方法だけを更新する。

**Rejected:**

- Version 1.4本文への部分追記で改版する方式
- Solution Partnerが完成実装コードを生成する方式
- サンプルコード等を行数や種類で全面禁止する方式
- UserがASKMEを保留した場合に作業停止を続ける方式
- 旧 `Design Response Check` を別章として残す方式
- Persona本文へ `Decision & Rationale` を追加する方式

### 2026-09-13

#### Education Code Generatorを Version 2.2へ更新し、Solution Partner v1.5の補足A 3区分へ整合

**Decision:**

Education用Code Generatorを Version 2.2 / Status Approved へ更新する。今回の目的はSolution Partner Version 1.5との引継ぎ整合に限定する。既存v2.1の未決事項制御思想は維持する。

- `補足A：未決事項一覧` の引継ぎインターフェースを、`次工程開始前に決定必須` / `実装段階まで持ち越し可能` / `仮定を置いて進めた事項` の3区分へ拡張する。
- `仮定` はUser確定仕様ではなく、Solution Partnerが明示した暫定実装条件として扱う。
- 明示された仮定があることだけを理由にコード生成を停止しない。
- Solution Partnerが具体的な暫定値・暫定方式を明示している場合は、その仮定に従って生成する。
- 仮定の内容が不明で実装条件を一意に決められない場合は、補完せずUserへ報告する。
- Code Generator自身による仮定作成は禁止する。
- 仮定を独自判断で変更・補完・確定しない。
- Mandatory Rulesと引き継いだ仮定が衝突する場合は、Mandatory Rulesを無視して実装せず、衝突内容をUserへ報告して停止する。
- 中核責務、Mandatory Rules 4-1〜4-5、Default Practices、実環境適用禁止、IDE / Git / test実行禁止、Evidence作成禁止、Reviewerとの責務境界、Solution Partner確定仕様を再設計しない原則は変更しない。
- 現行どおり、Persona本文へ `Decision & Rationale` は追加しない。判断履歴は本ファイルへ記録する。

**Reason:**

Solution Partner Version 1.5が `仮定を置いて進めた事項` を補足Aへ追加したため、Code Generatorが3区分を認識し、User確定仕様と暫定実装条件を混同せず受け取る必要がある。未決事項の勝手な補完禁止は維持しつつ、明示された仮定があることだけで初学者の実装工程を止めない。

**Rejected:**

- `仮定` があることだけを理由にコード生成を停止する方式
- `仮定` をUser確定仕様として扱う方式
- Code Generatorが新しい `仮定` を作成する方式
- Mandatory Rulesと衝突する仮定を、Mandatory Rulesを無視して実装する方式
- 未決事項制御思想を再設計する方式
- Persona本文へ `Decision & Rationale` を追加する方式
